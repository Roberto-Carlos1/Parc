import java.util.ArrayList;
import java.util.Vector;

public class Main {
    public static void main(String[] args) {
        // Vector<Animals> animaux = new Vector<>();
        // ArrayList<Carnivore> carnivores = new ArrayList<>();

        // animaux.add(new Carnivore("lion", 200.0, 400.0, false, new Points(5, 5), new
        // Points(2, 2)));
        // animaux.add(new Carnivore("lionne", 100, 300, false, new Points(80, 80), new
        // Points(2, 2)));
        // animaux.add(new Carnivore("puma", 150, 400, false, new Points(17, 17), new
        // Points(2, 2)));
        // animaux.add(new herbivore("zebre", 100, 300, false, new Points(80, 80), new
        // Points(2, 2)));
        // animaux.add(new herbivore("moutons", 150, 400, false, new Points(17, 17), new
        // Points(2, 2)));

        // Points[] point = new Points[animaux.size()];
        // point[0] = new Points(0, 0);
        // point[1] = new Points(12, 0);
        // point[2] = new Points(12, 7);
        // point[3] = new Points(7, 0);

        // Secteur secteur = new Secteur(point);

        // for (int i = 0; i < animaux.size(); i++) {

        // Points valiny = animaux.get(i).Deplacer(animaux.get(i).getPosition(),
        // animaux.get(i).getDeplacement());
        // System.out.println("x:" + valiny.getX() + "," + "y:" + valiny.getY());
        // }

    }
}